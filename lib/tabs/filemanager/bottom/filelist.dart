import 'package:flutter/material.dart';

class fileList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: 
          Text(
            'List of files shown here',
            style: TextStyle(fontSize: 20),
          ),
        ),
    );
  }
}